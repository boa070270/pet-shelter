import { PlainHtmlPipe } from './plain-html.pipe';

describe('PlainHtmlPipe', () => {
  it('create an instance', () => {
    const pipe = new PlainHtmlPipe();
    expect(pipe).toBeTruthy();
  });
});
