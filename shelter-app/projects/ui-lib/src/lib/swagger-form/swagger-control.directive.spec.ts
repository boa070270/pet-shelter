import { SwaggerControlDirective } from './swagger-control.directive';

describe('SwaggerControlDirective', () => {
  it('should create an instance', () => {
    const directive = new SwaggerControlDirective();
    expect(directive).toBeTruthy();
  });
});
