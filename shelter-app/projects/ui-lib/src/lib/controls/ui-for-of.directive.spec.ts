import { UiForOfDirective } from './ui-for-of.directive';

describe('UiForOfDirective', () => {
  it('should create an instance', () => {
    const directive = new UiForOfDirective();
    expect(directive).toBeTruthy();
  });
});
