import { TableControlDirective } from './table-control.directive';

describe('TableControlDirective', () => {
  it('should create an instance', () => {
    const directive = new TableControlDirective();
    expect(directive).toBeTruthy();
  });
});
