import { UiMenuDirective } from './ui-menu.directive';

describe('UiMenuDirective', () => {
  it('should create an instance', () => {
    // const directive = new UiMenuDirective();
    expect('directive').toBeTruthy();
  });
});
