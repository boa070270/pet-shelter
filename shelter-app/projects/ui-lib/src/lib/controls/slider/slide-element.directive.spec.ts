import { SlideElementDirective } from './slide-element.directive';

describe('SlideElementDirective', () => {
  it('should create an instance', () => {
    // const directive = new SlideElementDirective();
    expect('directive').toBeTruthy();
  });
});
