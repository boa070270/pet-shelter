import { ChoiceFormatPipe } from './choice-format.pipe';

describe('ChoiceFormatPipe', () => {
  it('create an instance', () => {
    const pipe = new ChoiceFormatPipe();
    expect(pipe).toBeTruthy();
  });
});
